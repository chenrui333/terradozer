name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write
  attestations: write
  id-token: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  goreleaser:
    name: GoReleaser
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Run GoReleaser
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: go run github.com/goreleaser/goreleaser/v2@v2.15.4 release --clean --timeout 20m

      - name: Attest release artifacts
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: |
            dist/terradozer_*_*_*.tar.gz
            dist/terradozer_*_checksums.txt

      - name: Download attestation bundles
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          for artifact in dist/terradozer_*_*_*.tar.gz dist/terradozer_*_checksums.txt; do
            gh attestation download "$artifact" --repo "${GITHUB_REPOSITORY}"
          done
          mkdir -p dist/attestations
          mv sha*.jsonl dist/attestations/

      - name: Upload attestation bundles to release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: dist/attestations/*.jsonl
          fail_on_unmatched_files: true
